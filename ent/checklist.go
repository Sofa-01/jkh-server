// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"jkh/ent/checklist"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Checklist is the model entity for the Checklist schema.
type Checklist struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Тип осмотра: весенний, зимний или частичный.
	InspectionType checklist.InspectionType `json:"inspection_type,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ChecklistQuery when eager-loading is set.
	Edges        ChecklistEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ChecklistEdges holds the relations/edges for other nodes in the graph.
type ChecklistEdges struct {
	// Elements holds the value of the elements edge.
	Elements []*ChecklistElement `json:"elements,omitempty"`
	// Tasks holds the value of the tasks edge.
	Tasks []*Task `json:"tasks,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ElementsOrErr returns the Elements value or an error if the edge
// was not loaded in eager-loading.
func (e ChecklistEdges) ElementsOrErr() ([]*ChecklistElement, error) {
	if e.loadedTypes[0] {
		return e.Elements, nil
	}
	return nil, &NotLoadedError{edge: "elements"}
}

// TasksOrErr returns the Tasks value or an error if the edge
// was not loaded in eager-loading.
func (e ChecklistEdges) TasksOrErr() ([]*Task, error) {
	if e.loadedTypes[1] {
		return e.Tasks, nil
	}
	return nil, &NotLoadedError{edge: "tasks"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Checklist) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case checklist.FieldID:
			values[i] = new(sql.NullInt64)
		case checklist.FieldTitle, checklist.FieldInspectionType, checklist.FieldDescription:
			values[i] = new(sql.NullString)
		case checklist.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Checklist fields.
func (_m *Checklist) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case checklist.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case checklist.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case checklist.FieldInspectionType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field inspection_type", values[i])
			} else if value.Valid {
				_m.InspectionType = checklist.InspectionType(value.String)
			}
		case checklist.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case checklist.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Checklist.
// This includes values selected through modifiers, order, etc.
func (_m *Checklist) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryElements queries the "elements" edge of the Checklist entity.
func (_m *Checklist) QueryElements() *ChecklistElementQuery {
	return NewChecklistClient(_m.config).QueryElements(_m)
}

// QueryTasks queries the "tasks" edge of the Checklist entity.
func (_m *Checklist) QueryTasks() *TaskQuery {
	return NewChecklistClient(_m.config).QueryTasks(_m)
}

// Update returns a builder for updating this Checklist.
// Note that you need to call Checklist.Unwrap() before calling this method if this Checklist
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Checklist) Update() *ChecklistUpdateOne {
	return NewChecklistClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Checklist entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Checklist) Unwrap() *Checklist {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Checklist is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Checklist) String() string {
	var builder strings.Builder
	builder.WriteString("Checklist(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("inspection_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.InspectionType))
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Checklists is a parsable slice of Checklist.
type Checklists []*Checklist
